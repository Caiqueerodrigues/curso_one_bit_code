import items from '../DataBase.json'

export default function GetItems () {
let produtos = null

    // if(localStorage.getItem('dadosEstoque') !== null) {
    //     let produtos = localStorage.getItem('dadosEstoque').json()
    //     alert('esta' + produtos)
    // } else {
    //     throw new Response('404 Not Found', { status: 1 })
    // }
    console.log(produtos)
    return produtos = 3;
}